export interface SocialFilter {
  pct_65_plus: { min: number; max: number };
  revenu_median_menage: { min: number; max: number };
  gini: { min: number; max: number };
  logement_reparations_majeures: { min: number; max: number };
}
